import { CustomMinEqualOptionalValidatorDirective } from './custom-min-equal-optional-validator.directive';

describe('CustomMinEqualOptionalValidatorDirective', () => {
  it('should create an instance', () => {
    const directive = new CustomMinEqualOptionalValidatorDirective();
    expect(directive).toBeTruthy();
  });
});
