import { CustomMaxEqualOptionalValidatorDirective } from './custom-max-equal-optional-validator.directive';

describe('CustomMaxEqualOptionalValidatorDirective', () => {
  it('should create an instance', () => {
    const directive = new CustomMaxEqualOptionalValidatorDirective();
    expect(directive).toBeTruthy();
  });
});
